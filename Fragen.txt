Fragen:

stone.py:
	warum set_color als eigene Methode? Falls das attribut in der Init gesetzt wird, lässt es sich doch dennoch ändern?

player.py:
	Man könnte hier in create_stones auch die Steine mit self.color initialisieren.